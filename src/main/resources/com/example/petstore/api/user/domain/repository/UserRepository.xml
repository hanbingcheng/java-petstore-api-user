<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.petstore.api.user.domain.repository.UserRepository">

    <select id="findUserByEmail">
        SELECT
            u.*
        FROM
            users u
        WHERE
            u.email = #{email}
    </select>

    <select id="findUserById">
        SELECT
        u.*
        FROM
        users u
        WHERE
        u.id = #{userId}
    </select>

    <insert id="insert">
        <selectKey keyProperty="id" resultType="long" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO users
        (
            username,
            first_name,
            last_name,
            email,
            phone,
            password
        )
        VALUES
        (
            #{username},
            #{firstName},
            #{lastName},
            #{email},
            #{phone},
            #{password}
        )
    </insert>

    <update id="update">
        UPDATE
            users
        SET
            username = #{username},
            first_name = #{firstName},
            last_name = #{lastName},
            phone = #{phone}
        WHERE
            id = #{id}
    </update>
</mapper>